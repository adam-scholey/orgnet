<Page
    x:Class="OrgNet.Desktop.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Channel List -->
        <Grid Grid.Column="0" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Channels" FontSize="18" FontWeight="SemiBold"/>

            <ListView x:Name="ChannelListView" Grid.Row="1" SelectionChanged="OnChannelSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="4">
                            <TextBlock FontWeight="SemiBold">
                                <Run Text="#"/>
                                <Run Text="{Binding Name}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding MessageCount}" FontSize="11" Opacity="0.5">
                                <Run Text="{Binding MessageCount}"/>
                                <Run Text="messages"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Chat Area -->
        <Grid Grid.Column="1" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE8BD;" FontSize="24"/>
                <TextBlock x:Name="ChannelHeader" Text="#general" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <InfoBar x:Name="ErrorBar" Grid.Row="1" Severity="Error" Title="Error" IsOpen="False" IsClosable="True"/>

            <ListView x:Name="MessageListView" Grid.Row="2" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="4,8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{Binding SenderName}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding SentAt}" FontSize="11" Opacity="0.4" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Message Input -->
            <Grid Grid.Row="3" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="MessageBox" Grid.Column="0" PlaceholderText="Type a message..." KeyDown="OnMessageKeyDown"/>
                <Button x:Name="SendButton" Grid.Column="1" Click="OnSendClicked" Style="{StaticResource AccentButtonStyle}">
                    <FontIcon Glyph="&#xE724;" FontSize="16"/>
                </Button>
            </Grid>
        </Grid>

        <ProgressBar x:Name="LoadingBar" Grid.ColumnSpan="2" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Top"/>
    </Grid>
</Page>
