<Page
    x:Class="OrgNet.Desktop.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon Glyph="&#xE716;" FontSize="22" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="User Management" Style="{StaticResource PageHeaderText}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Manage members and invitations" Style="{StaticResource PageSubHeaderText}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="8">
            <InfoBar x:Name="ErrorBar" Severity="Error" Title="Error" IsOpen="False" IsClosable="True"/>
            <InfoBar x:Name="StatusBar" Severity="Success" Title="Success" IsOpen="False" IsClosable="True"/>
        </StackPanel>

        <!-- Org Info -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20">
            <Grid ColumnSpacing="32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="OrgNameText" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="OrgPlanText" FontSize="13" Opacity="0.5"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock x:Name="MemberCountText" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Members" FontSize="12" Opacity="0.5" HorizontalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="2" Click="OnInviteClicked" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8FA;" FontSize="14"/>
                        <TextBlock Text="Invite User"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button Click="OnRefreshClicked">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Member List -->
        <ListView x:Name="MemberListView" Grid.Row="4" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,16" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20"
                                Background="{ThemeResource SystemAccentColorDark2}" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayName, Converter={x:Null}}" FontSize="16" FontWeight="Bold"
                                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding Email}" FontSize="12" Opacity="0.5"/>
                        </StackPanel>

                        <Border Grid.Column="2" CornerRadius="4" Padding="10,4" VerticalAlignment="Center"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <TextBlock Text="{Binding Role}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>

                        <TextBlock Grid.Column="3" Text="{Binding CreatedAt}" FontSize="11" Opacity="0.4" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressBar x:Name="LoadingBar" Grid.Row="4" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Top"/>
    </Grid>
</Page>
