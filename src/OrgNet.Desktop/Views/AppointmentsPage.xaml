<Page
    x:Class="OrgNet.Desktop.Views.AppointmentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon Glyph="&#xE787;" FontSize="22" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Appointments" Style="{StaticResource PageHeaderText}" VerticalAlignment="Center"/>
                <Button x:Name="RefreshButton" Click="OnRefreshClicked" VerticalAlignment="Center" Padding="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                </Button>
            </StackPanel>
            <TextBlock Text="Schedule and manage appointments" Style="{StaticResource PageSubHeaderText}"/>
        </StackPanel>

        <!-- Error Bar -->
        <InfoBar x:Name="ErrorBar" Grid.Row="1" Severity="Error" Title="Error" IsOpen="False" IsClosable="True"/>

        <!-- Create Appointment Form -->
        <Expander Grid.Row="2" Header="New Appointment" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" IsExpanded="False">
            <Grid Padding="8" RowSpacing="12" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="TitleBox" Grid.Row="0" Grid.ColumnSpan="2" Header="Title" PlaceholderText="Appointment title..."/>
                <TextBox x:Name="DescriptionBox" Grid.Row="1" Grid.ColumnSpan="2" Header="Description" PlaceholderText="Optional description..." AcceptsReturn="True" MinHeight="60"/>
                <TextBox x:Name="LocationBox" Grid.Row="2" Grid.ColumnSpan="2" Header="Location" PlaceholderText="Optional location..."/>

                <StackPanel Grid.Row="3" Grid.Column="0" Spacing="8">
                    <TextBlock Text="Start" FontWeight="SemiBold"/>
                    <DatePicker x:Name="StartDatePicker"/>
                    <TimePicker x:Name="StartTimePicker" ClockIdentifier="24HourClock"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="1" Spacing="8">
                    <TextBlock Text="End" FontWeight="SemiBold"/>
                    <DatePicker x:Name="EndDatePicker"/>
                    <TimePicker x:Name="EndTimePicker" ClockIdentifier="24HourClock"/>
                </StackPanel>

                <Button x:Name="CreateButton" Grid.Row="4" Grid.ColumnSpan="2" Content="Create Appointment"
                        Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Click="OnCreateClicked" Margin="0,8,0,0"/>
            </Grid>
        </Expander>

        <!-- Calendar + List (#7) -->
        <Grid Grid.Row="3" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Calendar View (#7) -->
            <CalendarView x:Name="AppointmentCalendar" Grid.Column="0"
                          CalendarViewDayItemChanging="OnCalendarDayItemChanging"
                          SelectedDatesChanged="OnCalendarDateSelected"
                          SelectionMode="Single"
                          MinWidth="300" MaxHeight="360"/>

            <!-- List -->
            <Grid Grid.Column="1" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                    <ComboBox x:Name="RangeCombo" SelectedIndex="1" SelectionChanged="OnRangeChanged" Width="140">
                        <ComboBoxItem Content="Today" Tag="today"/>
                        <ComboBoxItem Content="This Week" Tag="week"/>
                        <ComboBoxItem Content="This Month" Tag="month"/>
                        <ComboBoxItem Content="All" Tag="all"/>
                    </ComboBox>
                    <TextBlock x:Name="CountText" VerticalAlignment="Center" Opacity="0.5" FontSize="12"/>
                </StackPanel>

                <TextBlock x:Name="EmptyText" Grid.Row="1" Text="No appointments scheduled" Opacity="0.4" HorizontalAlignment="Center" Margin="0,40,0,0" Visibility="Collapsed"/>

                <ListView x:Name="AppointmentListView" Grid.Row="1" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12,8" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Date/Time badge -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" MinWidth="80">
                                <TextBlock Text="{Binding StartsAt}" FontSize="12" FontWeight="SemiBold"/>
                                <TextBlock FontSize="11" Opacity="0.5">
                                    <Run Text="to"/>
                                    <Run Text="{Binding EndsAt}"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Details -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <TextBlock Text="{Binding Location}" FontSize="12" Opacity="0.5"/>
                                    <TextBlock Text="{Binding CreatedBy}" FontSize="12" Opacity="0.4"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.6" TextWrapping="Wrap" MaxLines="2"/>
                            </StackPanel>

                            <!-- Cancel button -->
                            <Button Grid.Column="2" VerticalAlignment="Center" Click="OnCancelAppointmentClicked"
                                    Tag="{Binding Id}" ToolTipService.ToolTip="Cancel appointment">
                                <FontIcon Glyph="&#xE711;" FontSize="14"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

        <ProgressBar x:Name="LoadingBar" Grid.RowSpan="4" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Top"/>
    </Grid>
</Page>
