<Page
    x:Class="OrgNet.Desktop.Views.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Note List -->
        <Grid Grid.Column="0" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE70B;" FontSize="24"/>
                <TextBlock Text="Collaboration Notes" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Row="1" Click="OnNewNoteClicked" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE710;" FontSize="14"/>
                    <TextBlock Text="New Note"/>
                </StackPanel>
            </Button>

            <ListView x:Name="NoteListView" Grid.Row="2" SelectionChanged="OnNoteSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="4,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE840;" FontSize="12" Visibility="{Binding IsPinned}"/>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <TextBlock FontSize="11" Opacity="0.5">
                                <Run Text="{Binding CreatedBy}"/>
                                <Run Text="·"/>
                                <Run Text="{Binding CreatedAt}"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Note Editor -->
        <Grid Grid.Column="1" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <InfoBar x:Name="ErrorBar" Grid.Row="0" Severity="Error" Title="Error" IsOpen="False" IsClosable="True"/>
            <InfoBar x:Name="StatusBar" Grid.Row="0" Severity="Success" Title="Success" IsOpen="False" IsClosable="True"/>

            <TextBox x:Name="TitleBox" Grid.Row="1" Header="Title" PlaceholderText="Note title"/>

            <TextBox x:Name="ContentBox" Grid.Row="2" PlaceholderText="Write your note here..."
                     AcceptsReturn="True" TextWrapping="Wrap"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
                <CheckBox x:Name="PinnedCheck" Content="Pinned"/>
                <Button x:Name="SaveButton" Click="OnSaveClicked" Style="{StaticResource AccentButtonStyle}" Content="Save"/>
                <Button x:Name="DeleteButton" Click="OnDeleteNoteClicked" Content="Delete">
                    <Button.Foreground>
                        <SolidColorBrush Color="IndianRed"/>
                    </Button.Foreground>
                </Button>
                <TextBlock x:Name="EditorInfo" VerticalAlignment="Center" Opacity="0.4" FontSize="12"/>
            </StackPanel>
        </Grid>

        <ProgressBar x:Name="LoadingBar" Grid.ColumnSpan="2" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Top"/>
    </Grid>
</Page>
